package com.quantor.application.exchange;

/**
 * Supported exchange identifiers.
 *
 * Core/use-cases depend on this enum (not on any exchange SDK).
 */
public enum ExchangeId {
    BINANCE,
    BYBIT,
    OKX,
    COINBASE,
    PAPER,
    BACKTEST
}
