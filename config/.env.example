# Example only. Do NOT commit real secrets.
QUANTOR_MASTER_PASSWORD=change_me
SECRETS_MIGRATE_TO_DB=true

# --- API runtime (local) ---
# JWT signing secret (32+ chars).
QUANTOR_JWT_SECRET=CHANGE_ME_LONG_RANDOM

# Database connection for local Postgres (docker compose).
QUANTOR_DB_URL=jdbc:postgresql://127.0.0.1:5432/quantor
QUANTOR_DB_USER=quantor
QUANTOR_DB_PASSWORD=quantor

# Telegram (local). You MUST set chat id and bot token.
QUANTOR_TELEGRAM_BOT_TOKEN=
QUANTOR_TELEGRAM_CHAT_ID=
QUANTOR_TELEGRAM_ALLOWED_USER_IDS=
QUANTOR_TELEGRAM_ADMIN_CHAT_ID=
QUANTOR_TELEGRAM_ENABLED=true
