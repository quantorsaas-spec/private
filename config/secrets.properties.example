# Copy this file to: config/secrets.properties
# and fill values for your environment.
#
# IMPORTANT
# - Do NOT commit real secrets.
# - In GCP/Cloud Run we use Secret Manager (see deploy/gcp/*) and do NOT read this file.

# --- SaaS runtime secrets (Cloud Run / prod) ---
# Master password for admin/support operations.
QUANTOR_MASTER_PASSWORD=

# Database password for QUANTOR_DB_USER.
QUANTOR_DB_PASSWORD=

# Secret used to sign JWTs (HS256/HS512 etc). Use a long random string (32+ bytes).
QUANTOR_JWT_SECRET=

# Lemon Squeezy webhook signing secret.
QUANTOR_LEMONSQUEEZY_WEBHOOK_SECRET=

# --- Optional / local-only secrets ---
# If you store exchange API keys encrypted locally, set a strong key here.
# QUANTOR_ENCRYPTION_KEY=

# If you run a Telegram bot locally.
# QUANTOR_TELEGRAM_BOT_TOKEN=
# QUANTOR_TELEGRAM_CHAT_ID=
# QUANTOR_TELEGRAM_ALLOWED_USER_IDS=
# QUANTOR_TELEGRAM_ADMIN_CHAT_ID=

# Telegram keys consumed via ConfigPort.getSecret(...).
# These are read from this file (Java properties) and can be overridden by env vars.
#
# IMPORTANT:
# - Keep these lines present (even if empty) so env overrides work.
telegram.botToken=
telegram.chatId=
