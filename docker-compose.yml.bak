services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: quantor
      POSTGRES_USER: quantor
      POSTGRES_PASSWORD: quantor
    ports:
      - "5432:5432"
    volumes:
      - quantor_pgdata:/var/lib/postgresql/data

volumes:
  quantor_pgdata:
